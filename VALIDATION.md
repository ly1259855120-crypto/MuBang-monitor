# 验证记录

日期：2026-09-07。

- 本地Python 3.12虚拟环境安装基础依赖成功。
- 两源在线只读查询成功，各返回1条2026-09-05的603398公告。
- 没有使用真实SMTP账号，没有发送真实邮件。
- 没有创建GitHub仓库，没有向GitHub推送，没有声称已经在线部署。
- 可选模型未调用真实API；需用户配置服务商端点、模型和密钥。
- 14项离线测试全部通过：重启去重、跨源归并、PDF链接变化、不同编号、SMTP重试、主源故障备用源继续、心跳与次日日报、空结果、结构异常、分页、日期边界，以及AI失败隔离和摘要投递重试。
- 基础与可选依赖均可安装；Python编译检查通过；默认AI命令输出disabled，没有调用API。
- GitHub定时调度、仓库写权限、SMTP网络/授权码均需部署后手动运行验收。

运行离线测试：`python -m unittest discover -s tests -v`。
